extends ../layout.jade
block content
	for imagen in imagenes
		div(class="img-container")
			h2= imagen.title
			img(src="/public/imagenes/#{imagen._id}.#{imagen.extension}", calss="main-img")
			if(typeof imagen.creator != "undefined")
				p Por #{imagen.creator.email}